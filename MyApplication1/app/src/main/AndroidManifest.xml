<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="me.lianzhao.myapplication">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".CustomViewActivity"
            android:label="@string/title_activity_custom_view" />
        <activity
            android:name=".FragmentsActivity"
            android:label="@string/title_activity_fragments" />
        <activity
            android:name=".SlicesActivity"
            android:label="@string/title_activity_slices" />
        <activity
            android:name=".HlsPlayerActivity"
            android:label="@string/title_activity_hls_player" />
    </application>

</manifest>
